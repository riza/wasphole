instance:
  mode: linux
  industry: fintech
  seed: ""

ai:
  api_type: anthropic
  base_url: ""
  api_key: ""
  model: claude-sonnet-4-6
  cache_dir: /data/cache

transport:
  type: http
  port: 8080

alerts:
  sinks:
    - type: stdout
      enabled: true

canary:
  domain: ""
  http_callback: ""
  listen_addr: ""

session:
  log_dir: /data/sessions
